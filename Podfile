# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'IDMDataFlow' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IDMDataFlow
  pod 'IDMCore', '~> 0.1'
  pod 'Alamofire', '~> 3.4'
  pod 'SDWebImage', '~> 3.8'
  pod 'MBProgressHUD', '~> 1.0'

end

target 'IntegrationLayer' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IntegrationLayer
  pod 'IDMCore', '~> 0.1'

end

target 'ModelLayer' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ModelLayer
  pod 'IDMCore', '~> 0.1'

end

target 'DataLayer' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DataLayer
  pod 'IDMCore', '~> 0.1'
  pod 'Alamofire', '~> 3.4'

end
